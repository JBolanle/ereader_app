# Feature Specifications

This directory contains detailed specifications for features before implementation.

## Naming Convention

`[feature-name].md` - e.g., `epub-reader-mvp.md`, `bookmarks.md`

## Created By

Use the `/pm` command to generate specs:

```bash
claude /pm "Create spec for [feature description]"
```
